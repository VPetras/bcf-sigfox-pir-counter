# bcf-sigfox-pir-counter
This is repository for firmware which sends every 15 minutes message contains data of pir counter and temperature average. Message is posting via Sigfox to your backend. Then you need just decode data.
